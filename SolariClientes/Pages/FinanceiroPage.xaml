<Page
    x:Class="SolariClientes.Pages.FinanceiroPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SolariClientes.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <ListView x:Name="lstView" RequestedTheme="Light">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Height="50">
                        <TextBlock Text="{Binding DS_DOCUM}" MinWidth="300" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding DS_PARCELA}" MinWidth="150" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding DT_VENCIMENTO}" MinWidth="150" VerticalAlignment="Center"/>
                        <Button x:Name="btPagamento" Click="btPagamento_Click">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8C7;"/>
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
